/**
 *
 * @param {Array} resources - a collection of urls, File objects, or Images.
 * @param {Object} options - a configuration objec.
 */
export default function watertext(resources, options = {}) {
  return options;
}
